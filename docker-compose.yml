services:
  dbadmin:
    image: registry.cn-hangzhou.aliyuncs.com/redgreat/dbadmin:latest
    container_name: dbadmin
    hostname: dbadmin
    restart: unless-stopped
    environment:
      - PYTHONPATH=/opt/dbadmin
      - PYTHONUNBUFFERED=1
      - PYTHONTRACEMALLOC=25
      - PYTHONWARNINGS=default
      - TZ=Asia/Shanghai
      - LANG=zh_CN.UTF-8
    volumes:
      - ./log:/opt/dbadmin/log:rw
      - ./config/config.yml:/opt/dbadmin/config/config.yml:ro
      - ./data:/opt/dbadmin/data:rw
    ports:
      - "8501:80"
    networks:
      - dbadmin-network
    healthcheck:
      test: [ "CMD-SHELL", "curl -fsS http://localhost/ || exit 1" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    cpus: 1.0
    mem_limit: 2G
    mem_reservation: 64m
    deploy:
      resources:
        limits:
          cpus: 1.0
          memory: 2G
        reservations:
          memory: 64m
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    logging:
      driver: "json-file"

  celery-worker:
    image: registry.cn-hangzhou.aliyuncs.com/redgreat/dbadmin:latest
    container_name: dbadmin-celery-worker
    hostname: dbadmin-celery-worker
    restart: unless-stopped
    user: "1000:1000"
    working_dir: /opt/dbadmin
    command: ["celery", "-A", "app.core.celery_app.celery_app", "worker", "-l", "info", "-Q", "dbadmin.default", "--concurrency=2"]
    environment:
      - PYTHONPATH=/opt/dbadmin
      - PYTHONUNBUFFERED=1
      - TZ=Asia/Shanghai
      - LANG=zh_CN.UTF-8
    volumes:
      - ./log:/opt/dbadmin/log:rw
      - ./config/config.yml:/opt/dbadmin/config/config.yml:ro
      - ./data:/opt/dbadmin/data:rw
    networks:
      - dbadmin-network
    depends_on:
      dbadmin:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "/opt/dbadmin/script/celery-healthcheck.sh"]
      interval: 30s
      timeout: 15s
      retries: 3
      start_period: 30s
    cpus: 2.0
    mem_limit: 4G
    mem_reservation: 64m
    deploy:
      resources:
        limits:
          cpus: 2.0
          memory: 4G
        reservations:
          memory: 64m
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  dbadmin-network:
    driver: bridge
